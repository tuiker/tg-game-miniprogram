<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hou_tai.model.dao.SysMenuMapper">
    <select id="getSysMenuListByUserId" resultType="com.hou_tai.controller.pc.vo.SysMenuRespVO">
        SELECT sm.* FROM sys_role_menu AS srm LEFT JOIN sys_menu AS sm ON sm.id = srm.menu_id
        WHERE srm.role_id = (SELECT role_id FROM user_info WHERE id = #{userId})
        ORDER BY sm.sort ASC
    </select>
</mapper>